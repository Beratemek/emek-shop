{
  "name": "emek-shop-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm install --production && node index.js",
    "build": "cd server && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}
